# NanoShift

Android performance optimizer project. Build with the GitHub Actions workflow in `.github/workflows/build-apk.yml`.

The app uses Android-exposed performance controls and telemetry; it does not change the physical semiconductor process node.
